name: sp_3_mango

on:
  workflow_dispatch:  # Keeping manual trigger option
  schedule:
    # Runs at 00:00 UTC on Monday (3:00 AM Turkish time)
    - cron: '0 0 * * 1'

env:
  MONGODB_URL: ${{secrets.MONGODB_URL}}
  GH_TOKEN: ${{secrets.GH_TOKEN}}

jobs:
  dizitv_job:
    uses: ./.github/workflows/reusableProd.yml
    with:
      site: mango
      gitFolder: sponsor-product-3
    secrets: inherit