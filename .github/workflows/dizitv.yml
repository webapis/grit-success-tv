name: divitv
on:
   workflow_dispatch:
    inputs:
      site:
        description: 'site'
        required: true
        type: string
jobs:
  dizitv_job:
    uses: ./.github/workflows/reusable.yml
    with:
      site: ${{inputs.site}}
    secrets: inherit
