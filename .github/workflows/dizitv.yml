name: divitv
on:
   workflow_dispatch:
    inputs:
      site:
        description: 'site'
        required: true
        type: string
env:
  MONGODB_URL: ${{secrets.MONGODBURL}}
jobs:
  dizitv_job:
    uses: ./.github/workflows/reusable.yml
    with:
      site: ${{inputs.site}}
    secrets: inherit
